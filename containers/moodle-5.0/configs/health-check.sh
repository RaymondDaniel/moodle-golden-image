#!/bin/bash # Check if nginx is running if ! pgrep nginx > /dev/null; then echo "Nginx is not running" exit 1 fi # Check if php-fpm is running if ! pgrep php-fpm > /dev/null; then echo "PHP-FPM is not running" exit 1 fi # Check if Moodle is responding if ! curl -f -s http://localhost:8080/health > /dev/null; then echo "Moodle health check failed" exit 1 fi echo "All services healthy" exit 0
